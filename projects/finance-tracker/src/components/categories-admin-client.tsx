"use client";

import type { CategoryAdminItem } from "@/lib/finance-data";
import { useState } from "react";

type FormState = { name: string; slug: string; color: string; icon: string; isActive: boolean };

function formFromCategory(category?: CategoryAdminItem): FormState {
  return category ? { name: category.name, slug: category.slug, color: category.color ?? "#38bdf8", icon: category.icon ?? "", isActive: category.isActive } : { name: "", slug: "", color: "#38bdf8", icon: "", isActive: true };
}

export function CategoriesAdminClient({ initialCategories }: { initialCategories: CategoryAdminItem[] }) {
  const [categories, setCategories] = useState(initialCategories);
  const [editingId, setEditingId] = useState<string | null>(null);
  const [form, setForm] = useState<FormState>(formFromCategory());
  const [isModalOpen, setIsModalOpen] = useState(false);
  const [error, setError] = useState("");
  const [saving, setSaving] = useState(false);

  function openCreate() { setEditingId(null); setForm(formFromCategory()); setError(""); setIsModalOpen(true); }
  function openEdit(category: CategoryAdminItem) { setEditingId(category.id); setForm(formFromCategory(category)); setError(""); setIsModalOpen(true); }
  function closeModal() { if (saving) return; setIsModalOpen(false); setEditingId(null); setForm(formFromCategory()); setError(""); }

  async function submit(event: React.FormEvent<HTMLFormElement>) {
    event.preventDefault(); setSaving(true); setError("");
    try {
      const response = await fetch(editingId ? `/api/categories/${editingId}` : "/api/categories", { method: editingId ? "PATCH" : "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(form) });
      const result = await response.json() as { ok: boolean; category?: CategoryAdminItem; error?: string };
      if (!response.ok || !result.ok || !result.category) { setError(result.error ?? "Не удалось сохранить категорию"); return; }
      setCategories((current) => editingId ? current.map((item) => item.id === editingId ? result.category! : item) : [...current, result.category!].sort((a,b)=>a.name.localeCompare(b.name, 'ru')));
      closeModal();
    } catch { setError("Сбой сохранения. Повтори попытку."); } finally { setSaving(false); }
  }

  return <div className="space-y-4 sm:space-y-6"><section className="rounded-2xl border border-cyan-400/20 bg-cyan-400/10 p-4 shadow-lg shadow-black/20 sm:p-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><p className="text-xs uppercase tracking-[0.2em] text-cyan-200/80 sm:text-sm">Справочники</p><h2 className="mt-3 text-2xl font-semibold text-white sm:text-3xl">Категории</h2><p className="mt-3 text-sm leading-6 text-cyan-50/90">Здесь можно править названия, slug, цвета и активность категорий.</p></div><button onClick={openCreate} className="rounded-full bg-cyan-400 px-5 py-2 text-sm font-medium text-slate-950 transition hover:bg-cyan-300">+ Добавить категорию</button></div></section><section className="rounded-2xl border border-white/10 bg-white/5 p-4 shadow-lg shadow-black/20 sm:p-6"><div className="hidden overflow-hidden rounded-2xl border border-white/10 md:block"><table className="min-w-full divide-y divide-white/10 text-sm"><thead className="bg-white/5 text-left text-slate-400"><tr><th className="px-4 py-3 font-medium">Цвет</th><th className="px-4 py-3 font-medium">Название</th><th className="px-4 py-3 font-medium">Slug</th><th className="px-4 py-3 font-medium">Иконка</th><th className="px-4 py-3 font-medium">Статус</th><th className="px-4 py-3 font-medium text-right">Действия</th></tr></thead><tbody className="divide-y divide-white/10">{categories.map((category)=><tr key={category.id} className="text-slate-200"><td className="px-4 py-3"><span className="inline-flex h-6 w-6 rounded-full border border-white/10" style={{ backgroundColor: category.color ?? '#475569' }} /></td><td className="px-4 py-3 font-medium text-white">{category.name}</td><td className="px-4 py-3 text-slate-400">{category.slug}</td><td className="px-4 py-3 text-slate-400">{category.icon || '—'}</td><td className="px-4 py-3"><span className={`inline-flex rounded-full px-2.5 py-1 text-xs font-medium ${category.isActive ? 'bg-emerald-500/15 text-emerald-300' : 'bg-slate-500/20 text-slate-300'}`}>{category.isActive ? 'Активна' : 'Отключена'}</span></td><td className="px-4 py-3 text-right"><button onClick={() => openEdit(category)} className="rounded-full border border-white/10 px-3 py-2 text-sm text-slate-200 transition hover:border-cyan-400/50 hover:bg-cyan-400/10 hover:text-white">Изменить</button></td></tr>)}</tbody></table></div><div className="space-y-3 md:hidden">{categories.map((category)=><article key={category.id} className="rounded-xl border border-white/10 bg-slate-950/40 p-4"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><div className="flex items-center gap-3"><span className="inline-flex h-5 w-5 rounded-full border border-white/10" style={{ backgroundColor: category.color ?? '#475569' }} /><p className="font-medium text-white">{category.name}</p></div><p className="mt-1 text-xs text-slate-400">{category.slug}</p></div><button onClick={() => openEdit(category)} className="rounded-full border border-white/10 px-3 py-2 text-sm text-slate-200 transition hover:border-cyan-400/50 hover:bg-cyan-400/10 hover:text-white">Изменить</button></div></article>)}</div></section>{isModalOpen ? <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/80 p-4 backdrop-blur-sm"><div className="w-full max-w-md rounded-3xl border border-white/10 bg-slate-950 p-6 shadow-2xl shadow-black/30"><div className="flex items-start justify-between gap-3"><div><p className="text-xs uppercase tracking-[0.2em] text-cyan-300/80">Категория</p><h3 className="mt-2 text-xl font-semibold text-white">{editingId ? 'Изменить категорию' : 'Новая категория'}</h3></div><button onClick={closeModal} className="rounded-full border border-white/10 px-3 py-2 text-sm text-slate-200 transition hover:border-cyan-400/50 hover:bg-cyan-400/10 hover:text-white">✕</button></div><form onSubmit={submit} className="mt-5 space-y-4"><label className="block"><span className="mb-2 block text-sm text-slate-300">Название</span><input value={form.name} onChange={(event)=>setForm((current)=>({...current,name:event.target.value}))} className="w-full rounded-2xl border border-white/10 bg-slate-900 px-4 py-3 text-white outline-none transition focus:border-cyan-300/60" required /></label><label className="block"><span className="mb-2 block text-sm text-slate-300">Slug</span><input value={form.slug} onChange={(event)=>setForm((current)=>({...current,slug:event.target.value}))} className="w-full rounded-2xl border border-white/10 bg-slate-900 px-4 py-3 text-white outline-none transition focus:border-cyan-300/60" required /></label><div className="grid gap-4 sm:grid-cols-2"><label className="block"><span className="mb-2 block text-sm text-slate-300">Цвет</span><input type="color" value={form.color} onChange={(event)=>setForm((current)=>({...current,color:event.target.value}))} className="h-12 w-full rounded-2xl border border-white/10 bg-slate-900 px-2 py-2" /></label><label className="block"><span className="mb-2 block text-sm text-slate-300">Иконка</span><input value={form.icon} onChange={(event)=>setForm((current)=>({...current,icon:event.target.value}))} className="w-full rounded-2xl border border-white/10 bg-slate-900 px-4 py-3 text-white outline-none transition focus:border-cyan-300/60" placeholder="🍔" /></label></div><label className="flex items-center gap-3 rounded-2xl border border-white/10 bg-slate-900 px-4 py-3 text-sm text-slate-300"><input type="checkbox" checked={form.isActive} onChange={(event)=>setForm((current)=>({...current,isActive:event.target.checked}))} /> Активная категория</label>{error ? <div className="rounded-2xl border border-rose-400/30 bg-rose-500/10 px-4 py-3 text-sm text-rose-200">{error}</div> : null}<div className="flex gap-3"><button type="button" onClick={closeModal} className="flex-1 rounded-2xl border border-white/10 px-4 py-3 text-sm text-slate-200 transition hover:border-cyan-400/50 hover:bg-cyan-400/10 hover:text-white">Отмена</button><button type="submit" disabled={saving} className="flex-1 rounded-2xl bg-cyan-400 px-4 py-3 text-sm font-medium text-slate-950 transition hover:bg-cyan-300 disabled:cursor-not-allowed disabled:opacity-60">{saving ? 'Сохраняю...' : editingId ? 'Сохранить' : 'Добавить'}</button></div></form></div></div> : null}</div>;
}
